# Projects Service

Сервис для управления проектами в Finder приложении.

## Типы файлов

Сервис поддерживает следующие типы файлов:

- `folder` / `folder-filled` - папки
- `readme` - README.md файлы с документацией
- `architecture` - файлы архитектуры (excalidraw)
- `demo` - ссылки на демо проектов
- `github` - ссылки на GitHub репозитории
- `database` - диаграммы базы данных (dbdiagram)
- `swagger` - Swagger документация (только просмотр)

## Сохранение файлов

### Структура данных

Каждый проект имеет поле `url` (тип `String` в БД), которое хранит ссылку на файл в MinIO или внешнюю ссылку. Это соответствует нормализации БД (БКНФ) - данные файлов хранятся в MinIO, а в PostgreSQL только ссылка.

### Формат URL

URL может быть:
- **MinIO URL**: `https://minio.example.com/bucket/path/to/file.ext`
- **Внешняя ссылка**: `https://example.com/demo`, `https://github.com/user/repo`

### Логика сохранения в Consumer

Consumer автоматически устанавливает `url` из следующих источников (в порядке приоритета):

1. **file_info** (загруженный файл через admin service)
   - Если файл загружен, используется его URL из `file_info['url']`

2. **url в data**
   - Если `url` передан напрямую в `data`, используется он

### Примеры использования

#### Создание проекта с загруженным файлом
```json
{
  "service": "projects",
  "payload": {
    "type": "create_project",
    "data": {
      "name": "README.md",
      "type": "file",
      "icon": "📝",
      "file_type": "readme"
    }
  },
  "file": {
    "name": "readme",
    "extension": "md",
    "path": "",
    "content": "base64..."
  }
}
```

Consumer автоматически установит:
```json
{
  "url": "https://minio.example.com/projects/readme.md"
}
```

#### Создание проекта с прямой ссылкой
```json
{
  "service": "projects",
  "payload": {
    "type": "create_project",
    "data": {
      "name": "Demo",
      "type": "file",
      "icon": "🌐",
      "file_type": "demo",
      "url": "https://example.com/demo"
    }
  }
}
```

Consumer использует переданный `url` как есть.

## Gateway

Gateway получает проекты через gRPC и передает `url` как строку в ответе API. Frontend использует URL напрямую для открытия файлов.

## gRPC

gRPC сервис передает `url` как `optional string` в proto сообщении `Project`. Данные корректно сериализуются и десериализуются через `project_model_to_proto` и `proto_project_to_schema`.

